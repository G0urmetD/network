# start the VM
# clone the network.sh and the server-openvpn.sh
# chmod +x network.sh
# chmod +x server-openvpn.sh

# run network.sh -> machine will reboot after
# ./network.sh
# set network settings to: Networkbridge (schoolnetwork)

# run the server-openvpn.sh
# ./server-openvpn.sh
# lsof -i -P -> checks if the service is runnning
# reboot

# start a new VM for the Client
# clone the client-openvpn.sh
# chmod +x client-openvpn.sh
# ./client-openvpn.sh
# lsof -i -P -> checks if the service is running

# check if ping is running on both sites
